# EPMO — Efficient PyTorch Multi-worker Optimizer
# Core dependencies

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# Optional but strongly recommended
xgboost>=2.0.0

# Required for predict.py (auto hardware detection)
psutil>=5.9.0
torch>=2.0.0
